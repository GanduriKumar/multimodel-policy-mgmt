# Example environment variables for the Vite React frontend
#
# Copy this file to `.env.local` (not committed) and adjust values as needed.

# Base URL for the backend API (include /api prefix if your server exposes it)
VITE_API_BASE_URL=http://localhost:8000/api

# Client API key, if your backend requires an API key header
# Do NOT commit real keys. Override in .env.local
VITE_API_KEY=changeme
